# Backend placeholder

Put your server implementation in this folder.

- `server.example.js` contains a minimal local HTTP example.
- Update endpoint logic to match your AI processing node URLs.
- The example limits request bodies to 1MB for safety.
